
namespace framework.os.threading {

	public interface Thread
	
	{
	  // --- Receptions ---
	
	  // --- Operations ---
		void start(global::framework.os.threading.Runnable runnable);
		void join();
		void interrupt();
	
	  // --- Properties ---
        global::framework.basicTypes.Boolean isRunning { get; }
	}

}
